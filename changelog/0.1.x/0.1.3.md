---
summary: "Error code semantics for domain validation; mcp-ts-core ^0.9.5 → ^0.9.6."
breaking: false
security: false
---

# 0.1.3 — 2026-05-23

## Changed

- `@cyanheads/mcp-ts-core` `^0.9.5 → ^0.9.6`.
- Skills synced: `polish-docs-meta` 2.1→2.2, `release-and-publish` 2.4→2.5.
- `scripts/lint-packaging.ts` — added manifest `name` scope check and `user_config` key validation.

## Fixed

- `biorxiv-get-preprint` — `invalid_doi_format` error now uses `ValidationError` code instead of `InvalidParams`.
- `biorxiv-get-published-version` — `invalid_doi_format` error now uses `ValidationError` code instead of `InvalidParams`.
- `biorxiv-list-recent` — `invalid_date_range` and `invalid_category` errors now use `ValidationError` code instead of `InvalidParams`.

## Added

- `LICENSE` — Apache-2.0 license file.
