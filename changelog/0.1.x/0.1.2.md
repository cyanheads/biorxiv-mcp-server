---
summary: "mcp-ts-core ^0.9.5, MCPB bundle support, optional-chain lint fixes."
breaking: false
security: false
---

# 0.1.2 — 2026-05-23

## Added

- MCPB bundle support: `manifest.json`, `.mcpbignore`, and `bundle`/`lint:packaging`/`audit:refresh`/`publish-mcp` scripts for one-click Claude Desktop install.
- `.env.example` with `BIORXIV_MAILTO` and optional `BIORXIV_API_BASE_URL` / `EUROPEPMC_API_BASE_URL` entries.
- README install badges (Claude Desktop, Cursor, VS Code) and repositioned framework badge.
- `scripts/lint-packaging.ts` — verifies env var alignment between `manifest.json` and `server.json` (run by `devcheck`).

## Changed

- `@cyanheads/mcp-ts-core` `^0.9.1 → ^0.9.5`.
- `zod` `^4.4.3` added as explicit dependency (was transitive).
- `biorxiv-list-recent.tool.ts`, `biorxiv-service.ts` — Biome optional-chain fixes.
- `scripts/devcheck.ts` updated; `scripts/lint-packaging.ts` added.
- Skills synced: `field-test` 2.4→2.5, `maintenance` 2.1→2.4, `polish-docs-meta` 1.8→2.1, `release-and-publish` 2.2→2.4.
- CLAUDE.md: framework version, server version, bundling section, commands table updated.
