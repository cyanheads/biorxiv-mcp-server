---
summary: "Pre-launch polish: code simplification in list-recent tool and Europe PMC service, Docker docs added, AGENTS.md added, import order and comment cleanup."
breaking: false
---

# 0.1.5 — 2026-05-23

## Changed

- **`biorxiv-list-recent.tool.ts`** — extracted duplicated `nextCursor` calculation into a `toPaginationEntry` helper, eliminating three copies of the same block; widened `PreprintForFormat` mapped type to drop the unnecessary `doi`-specific branch.
- **`europe-pmc-service.ts`** — replaced the imperative `for` loop over raw results with a `filter`+`map` chain using a type predicate; removed two inline comments that added no signal; moved `import` statements before the `asRc` helper to match the module's natural declaration order.
- **`biorxiv-service.ts`** — moved the `asRc` helper below framework imports to align with module declaration order; tightened the comment to remove the cross-file reference.
- **`biorxiv-list-recent.tool.ts`** — updated `canvas_id` field description to clarify the `CANVAS_PROVIDER_TYPE=duckdb` prerequisite.
- **`README.md`** — added Docker usage section with build/run example and Dockerfile defaults; reordered badge row to match standard layout (Version, License, Docker, MCP SDK, npm, TypeScript, Bun).
- **`AGENTS.md`** — added as a copy of `CLAUDE.md` for non-Claude agent runtimes; included in `package.json` `files[]`.
- **`.env.example`** — added DataCanvas env var comment block.
- **`bunfig.toml`** — added Bun workspace/test configuration.
- **`Dockerfile`** — minor update.
